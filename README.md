# My OpenSkills

개인 커스텀 OpenSkills 컬렉션

## 스킬 목록

| 스킬 | 설명 |
|-----|------|
| `blender-glb-alignment` | Blender MCP로 GLB 모델을 reference 모델에 자동 정렬 (WebAR용) |

## 설치 방법

```bash
# Public repo (저장소를 public으로 설정 필요)
openskills install Opticmix/my-openskills

# 로컬 경로에서 설치
openskills install /path/to/my-openskills
```